describe(('Movie') => {

  it('Should fetch a movie', () => {

  })

  it('Should clean up the year', () => {
    const date = 
    cleanYear.instance()

  })

  it('Should create a object that holds movie information', () => {

  })

  it('Should call clean year function and pass the movie date', () => {

  })
})
